apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "harp-proxy.fullname" . }}
data:
  harp.yaml: |
    proxy:
      endpoints:
        {{- range .Values.endpoints }}
        "{{ .port }}":
          name: "{{ .name }}"
          target: "{{ .url }}"
        {{- end }}
