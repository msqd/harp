DEFAULT_SYSTEM_CONFIG_FILENAMES = (
    "/etc/harp.yaml",
    "/etc/harp.yml",
)

DEFAULT_APPLICATIONS = (
    "http_client",
    "http_cache",
    "proxy",
    "storage",
    "dashboard",
    "janitor",
    "rules",
)
