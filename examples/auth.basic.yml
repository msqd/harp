# extends: sqlite.yml

storage:
  type: sqlalchemy
  url: sqlite+aiosqlite:///harp.db
  drop_tables: true
  echo: false

proxy:
  endpoints:
    4000:
      name: httpbin
      url: http://httpbin.org

dashboard:
  auth:
    type: basic
    users:
      admin: { password: admin }
      charles: { password: mingus }
      miles: { password: davis }
      john: { password: coltrane }
