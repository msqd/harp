# extends: sqlite.yaml

extensions:
  - harp.contrib.cookie_based_stupid_auth

stupid_token: foo bar baz

storage:
  type: sqlalchemy
  url: sqlite+aiosqlite:///harp.db
  drop_tables: true
  echo: false

proxy:
  endpoints:
    4000:
      name: httpbin
      url: http://httpbin.org
