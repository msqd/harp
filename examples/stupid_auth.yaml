# extends: sqlite.yaml

storage:
  type: sqlalchemy
  url: sqlite+aiosqlite:///harp.db
  drop_tables: true
  echo: false

proxy:
  endpoints:
    4000:
      name: httpbin
      url: http://httpbin.org

dashboard:
  auth: foo bar baz
