Install System Dependencies on Ubuntu 24.04 / Noble Numbat (LTS)
================================================================

To set up a new Ubuntu Server machine, you can use the following script.

It will install all the necessary prerequisites to download, run, and test HARP on the machine.

.. literalinclude:: ../../../misc/ubuntu/install.sh
    :language: shell
